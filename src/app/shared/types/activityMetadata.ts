export interface ActivityMetadata {
    activityName: string;
    numDays: number; 
    numRecords: number;
    firstRecordDate: Date;
    lastRecordDate: Date;
    standardDeviation: number;
    variablility: number; 
}